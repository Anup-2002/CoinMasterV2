{
  "entities": {
    "SessionState": {
      "title": "SessionState",
      "description": "Cookie state JSON for automated Playwright browser login session",
      "type": "object",
      "properties": {
        "stateJson": {
          "type": "string",
          "description": "Stringified state.json from Playwright"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": ["stateJson", "updatedAt"]
    },
    "TrendingCoin": {
      "title": "TrendingCoin",
      "description": "A cryptocurrency scraped from CoinMarketCap trending page",
      "type": "object",
      "properties": {
        "rank": { "type": "integer" },
        "name": { "type": "string" },
        "symbol": { "type": "string" },
        "url": { "type": "string" },
        "lastUpdated": { "type": "string", "format": "date-time" }
      },
      "required": ["rank", "name", "symbol", "url"]
    },
    "GeneratedMessage": {
      "title": "GeneratedMessage",
      "description": "AI generated bullish/bearish comment for a specific coin",
      "type": "object",
      "properties": {
        "coinName": { "type": "string" },
        "symbol": { "type": "string" },
        "url": { "type": "string" },
        "sentiment": { "type": "string" },
        "message": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["coinName", "symbol", "sentiment", "message"]
    },
    "PostResult": {
      "title": "PostResult",
      "description": "Result entry for an automated comment posting run",
      "type": "object",
      "properties": {
        "timestamp": { "type": "string" },
        "coinName": { "type": "string" },
        "symbol": { "type": "string" },
        "sentiment": { "type": "string" },
        "status": { "type": "string" },
        "message": { "type": "string" }
      },
      "required": ["timestamp", "coinName", "symbol", "status", "message"]
    },
    "BotProgress": {
      "title": "BotProgress",
      "description": "Current execution state and progress tracker of the automation bot",
      "type": "object",
      "properties": {
        "nextIndex": { "type": "integer" },
        "isRunning": { "type": "boolean" },
        "botStatus": { "type": "string" },
        "lastUpdated": { "type": "string", "format": "date-time" }
      },
      "required": ["nextIndex", "isRunning", "botStatus"]
    },
    "SystemLog": {
      "title": "SystemLog",
      "description": "High-fidelity execution logs generated by the system",
      "type": "object",
      "properties": {
        "timestamp": { "type": "string" },
        "level": { "type": "string" },
        "message": { "type": "string" },
        "createdAt": { "type": "integer" }
      },
      "required": ["timestamp", "level", "message", "createdAt"]
    }
  },
  "firestore": {
    "/bot/session": {
      "schema": "SessionState",
      "description": "Single document representing the current browser login session cookies"
    },
    "/bot/progress": {
      "schema": "BotProgress",
      "description": "Single document tracking active bot execution state and queue progress"
    },
    "/coins/{coinId}": {
      "schema": "TrendingCoin",
      "description": "Collection of current trending coins"
    },
    "/messages/{messageId}": {
      "schema": "GeneratedMessage",
      "description": "Collection of generated bullish/bearish messages for trending coins"
    },
    "/results/{resultId}": {
      "schema": "PostResult",
      "description": "Collection of automated posting outcome results"
    },
    "/logs/{logId}": {
      "schema": "SystemLog",
      "description": "Collection of system logs for browser console reporting"
    }
  }
}
